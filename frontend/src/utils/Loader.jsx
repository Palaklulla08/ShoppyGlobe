import React from 'react';

/**
 * Loader component
 * - Simple reusable component to show a loading indicator
 * - Can be used anywhere data is being fetched
 */
export default function Loader() {
  return (
    <div className="center">
      Loading…
    </div>
  );
}
